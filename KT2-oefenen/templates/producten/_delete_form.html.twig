<form method="post" action="{{ path('producten_delete', {'id': producten.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producten.id) }}">
    <button class="btn">Delete</button>
</form>
