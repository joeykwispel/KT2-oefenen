{% extends 'base.html.twig' %}

{% block title %}SubProducten{% endblock %}

{% block body %}
    <h1>SubProducten</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_producten.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ sub_producten.naam }}</td>
            </tr>
            <tr>
                <th>Omschrijving</th>
                <td>{{ sub_producten.omschrijving }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sub_producten_index') }}">back to list</a>

    <a href="{{ path('sub_producten_edit', {'id': sub_producten.id}) }}">edit</a>

    {{ include('sub_producten/_delete_form.html.twig') }}
{% endblock %}
